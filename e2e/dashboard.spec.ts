import { test, expect } from '@playwright/test'

test.describe('Dashboard', () => {
  test('placeholder - dashboard loads', async () => {
    // TODO: Launch Electron app and verify the dashboard view renders
    expect(true).toBe(true)
  })
})
