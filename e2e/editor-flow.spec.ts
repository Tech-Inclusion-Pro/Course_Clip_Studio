import { test, expect } from '@playwright/test'

test.describe('Editor Flow', () => {
  test('placeholder - editor opens and renders canvas', async () => {
    // TODO: Launch Electron app, open a course, and verify the editor view renders
    expect(true).toBe(true)
  })
})
